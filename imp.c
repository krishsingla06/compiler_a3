// for now we are spilling aa the regs before call and after return
// other approache might be freeing up all the regs, store both descriptor files, then clear regs files and after calling, take back both descriptor files and continue

x++ for floats mei abhi type conversion baaki hai

also there is nothing like pointers to arrays
toh decay of array to pointer is simply assigning the base address of array to pointer variable